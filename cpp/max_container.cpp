#include <iostream>
#include <cstdlib>
using namespace std;

// problem:
// an int array {a[0], a[1], a[2]..., a[n]}
// find two index: i < j, which will have the value of
// X = max((j-i) * max(a[i], a[j]))
// input:  the array of int a[n];
// output: the max value of X
int main()
{
    return 0;
}
